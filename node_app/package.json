{
  "author": "",
  "babel": {
    "plugins": [
      "@babel/plugin-proposal-optional-chaining"
    ],
    "presets": []
  },
  "description": "",
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "@babel/plugin-proposal-optional-chaining": "^7.2.0",
    "babelify": "^10.0.0",
    "browserify": "^16.2.3",
    "esmify": "^2.1.1",
    "uglify-es": "^3.3.9"
  },
  "license": "ISC",
  "main": "app.js",
  "name": "node_app",
  "scripts": {
    "bundle-js": "browserify --plugin=esmify --transform [ babelify ] --entry app.js | uglifyjs --mangle --compress > dist/bundle.min.js",
    "clean": "rm -rf dist && mkdir -p dist",
    "prestart": "npm run clean && npm run bundle-js",
    "start": "node dist/bundle.min.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "version": "0.0.1"
}
